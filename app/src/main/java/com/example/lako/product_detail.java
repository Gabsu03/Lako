/* package com.example.lako;

import android.os.Bundle;

import android.content.Intent;
import android.os.Bundle;
import android.widget.ImageView;
import android.widget.TextView;

import androidx.appcompat.app.AppCompatActivity;

public class product_detail extends AppCompatActivity {

    private ImageView productImage, sellerProfileImage;
    private TextView productName, productPrice, productSold, productDescription;
    private TextView productSpecifications, productCategory, productSellerName;

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        // setContentView(R.layout.activity_product_detail);

        // Initialize views
        // productImage = findViewById(R.id.product_image_detail);
        // productName = findViewById(R.id.product_name_detail);
        // productPrice = findViewById(R.id.product_price_detail);
        /* productSold = findViewById(R.id.product_sold_detail);
        productDescription = findViewById(R.id.product_description_detail);
        productSpecifications = findViewById(R.id.product_specifications_detail);
        productCategory = findViewById(R.id.product_category_detail);
        productSellerName = findViewById(R.id.seller_name);
        sellerProfileImage = findViewById(R.id.seller_profile_image);

        // Get product data from the intent
        Intent intent = getIntent();
        String name = intent.getStringExtra("name");
        int imageResource = intent.getIntExtra("image", 0);
        String price = intent.getStringExtra("price");
        String sold = intent.getStringExtra("sold");
        String description = intent.getStringExtra("description");
        String specifications = intent.getStringExtra("specifications");
        String category = intent.getStringExtra("category");
        String sellerName = intent.getStringExtra("sellerName");
        int sellerImageResource = intent.getIntExtra("sellerImage", 0);

        // Set product details
        productImage.setImageResource(imageResource);
        productName.setText(name);
        productPrice.setText(price);
        productSold.setText(sold);
        productDescription.setText(description);
        productSpecifications.setText(specifications);
        productCategory.setText(category);
        productSellerName.setText(sellerName);
        sellerProfileImage.setImageResource(sellerImageResource);
    }
} */